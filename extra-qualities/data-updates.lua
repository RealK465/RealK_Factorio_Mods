-- Everything here edits prototypes owned by base, quality or space-age, so it waits for
-- data-updates. space-age does its own quality rewiring from its data.lua, and would
-- otherwise overwrite the technology changes below.
require("prototypes.quality.ladder")
require("prototypes.quality.vanilla-technology")
require("prototypes.quality.icons")
