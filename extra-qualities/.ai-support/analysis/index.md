# analysis index — Extra Qualities

Verified engine findings, one file per topic, each carrying the version it was checked at.

- **`quality-roll-mechanics.md`** — how the engine actually rolls quality: the formula, what
  `next_probability` above 1 does, and what happens to the probability of a tier the force has
  not researched. Read before changing any odds, and before trusting the cost model in
  `../balance.md`.
