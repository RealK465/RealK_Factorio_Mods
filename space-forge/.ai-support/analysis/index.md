# analysis — Space Forge

Evidence: facts checked against the installed game rather than recalled. Every file carries
`verified_against` front matter naming the version it was checked at, and every claim inside
carries its own confidence marker — **Verified**, **Measured**, or **UNVERIFIED** in those
words. Re-verify and edit in place when the dev install moves; the repo `CLAUDE.md` →
*AI support folders* has the rules and `../../../.claude/references/ai-support.md` the detail.

| File | Verified against | What is in it | Read it when |
|---|---|---|---|
| `vanilla-palette.md` | 2.1.14 | measured paint and emissive colour across base and Space Age machines: how little paint a Space Age entity actually carries, which glow colours are taken, and which band is free | before choosing any colour for a machine, an icon or a glow — and before assuming a vanilla entity's identity colour is what you remember it being |

**Nothing else is here yet.** The mod has no code, so there is nothing about the engine to
verify. Add a file the first time an engine fact has to be established rather than recalled —
a prototype field's real behaviour, a crafting-category limit, how a recipe interacts with
Space Age's planet gating — and index it here in the same change.
